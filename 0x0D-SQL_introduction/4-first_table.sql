-- Creates a new table in the current database
-- Query that creates table first_table if it doesn't exist in current database in MySQL
CREATE TABLE IF NOT EXISTS first_table(
id INT,
name VARCHAR(256)
);
